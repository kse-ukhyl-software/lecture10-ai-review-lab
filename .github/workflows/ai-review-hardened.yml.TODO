# TODO -- your hardened workflow goes here.
#
# Rename this file to ai-review-hardened.yml and replace the placeholder
# below with a working implementation. Delete ai-review-insecure.yml in the
# same PR.
#
# Requirements (see TASKS.md, Task 3):
#
# [ ] Trigger on pull_request, not pull_request_target.
# [ ] Explicit permissions block: contents: read, pull-requests: write.
# [ ] timeout-minutes on the job.
# [ ] actions/checkout with a bounded fetch-depth (2 is usually enough).
# [ ] API key loaded only in the step that calls the model.
# [ ] Model response validated against a JSON schema. On schema mismatch,
#     fail the step with a clear error. Do not post a comment built from
#     unvalidated text.
# [ ] Findings filter that drops low-impact noise (follow the pattern in
#     anthropics/claude-code-security-review/claudecode/findings_filter.py).
# [ ] Approval gate for PRs from external contributors (see the reference
#     repo README section titled "Security Considerations").
#
# Cite the specific file or pattern you reused from the reference repo in
# a comment at the top of your workflow. For example:
#
#   # Based on anthropics/claude-code-security-review commit <sha>
#   # Reused patterns:
#   #   - findings_filter.py -> filter_low_severity helper
#   #   - action.yml       -> input validation and claudecode-timeout default
#   #   - README           -> approval gate for external contributors

name: AI Review (hardened -- UNIMPLEMENTED)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Not implemented. See TASKS.md Task 3."
          exit 1
